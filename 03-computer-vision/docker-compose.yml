services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Mount host models/ so pretrained weights survive container restarts without re-downloading
      - ./models:/app/models
