# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.9.0

# Computer Vision — core inference
torch>=2.5.0
torchvision>=0.20.0
opencv-python>=4.10.0
numpy>=2.0.0
# Pillow: required by torchvision.transforms.ToPILImage() used in detector.py preprocessing
pillow>=11.0.0

# Visualization — MetricsVisualizer (visualization.py) uses matplotlib for training curves;
# seaborn is used specifically for sns.heatmap() in plot_confusion_matrix(); neither is in the live inference path
matplotlib>=3.9.0
seaborn>=0.13.0

# Testing & Quality
pytest>=8.2.0
pytest-cov>=5.0.0
ruff>=0.6.0
httpx>=0.27.0
